name: GitHub Actions Demo
on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        run:  echo "Added branch filters."
